---
percona_postgresql_packages:
  - acl
  - "percona-postgresql-{{ percona_postgresql_version }}-main"
  - "percona-postgresql-{{ percona_postgresql_version }}-pgaudit"
  - percona-patroni
  - "percona-pg-stat-monitor{{ percona_postgresql_version }}"
  - percona-postgresql-contrib

# WAL-G
# percona_postgresql_walg_binary: wal-g-pg-linux-amd64.tar.gz
