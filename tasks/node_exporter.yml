---
- name: Create node_exporter group
  ansible.builtin.group:
    name: "{{ percona_postgresql_node_exporter_group }}"
    system: true

- name: Create node_exporter user
  ansible.builtin.user:
    name: "{{ percona_postgresql_node_exporter_user }}"
    group: "{{ percona_postgresql_node_exporter_group }}"
    shell: /usr/sbin/nologin
    create_home: false
    system: true

- name: Download Node Exporter archive
  ansible.builtin.get_url:
    url: "{{ percona_postgresql_node_exporter_download_url }}"
    dest: "{{ percona_postgresql_node_exporter_download_dest }}"
    mode: "0644"

- name: Extract Node Exporter archive
  ansible.builtin.unarchive:
    src: "{{ percona_postgresql_node_exporter_download_dest }}"
    dest: "/tmp"
    remote_src: true
    creates: "/tmp/node_exporter-{{ percona_postgresql_node_exporter_version }}.{{ percona_postgresql_node_exporter_arch }}"

- name: Install Node Exporter binary
  ansible.builtin.copy:
    src: "/tmp/node_exporter-{{ percona_postgresql_node_exporter_version }}.{{ percona_postgresql_node_exporter_arch }}/node_exporter"
    dest: "{{ percona_postgresql_node_exporter_binary }}"
    owner: "{{ percona_postgresql_node_exporter_user }}"
    group: "{{ percona_postgresql_node_exporter_group }}"
    mode: "0755"
    remote_src: true
  notify: Restart node_exporter

- name: Deploy node_exporter systemd service
  ansible.builtin.template:
    src: node_exporter.service.j2
    dest: /etc/systemd/system/node_exporter.service
    owner: root
    group: root
    mode: "0644"
  notify:
    - Reload systemd
    - Restart node_exporter

- name: Enable and start node_exporter
  ansible.builtin.systemd:
    name: node_exporter
    enabled: true
    state: started
